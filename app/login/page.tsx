"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";export default function Login(){const r=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);try{const res=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await res.json();if(!res.ok)throw new Error(data.error||"Unable to sign in");r.push("/dashboard");r.refresh()}catch(err){setError(err instanceof Error?err.message:"Unable to sign in")}finally{setLoading(false)}}return <main className="login"><div className="login-card"><div className="login-brand"><b>C</b><span>CREDLOCK<small>AFRICA · SIWES PORTAL</small></span></div><label>SECURE ACCESS</label><h1>Welcome back</h1><p>Sign in to continue to your workspace.</p><form onSubmit={submit}><input required type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/><input required type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<small className="error">{error}</small>}<button className="login-btn" disabled={loading}>{loading?"Signing in…":"Sign in"}</button></form><small>Use an account provisioned in the portal database.</small></div></main>}