"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{ShieldCheck,ArrowRight,LockKeyhole}from"lucide-react";export default function Login(){const r=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");setLoading(true);try{const res=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await res.json();if(!res.ok)throw new Error(data.error||"Unable to sign in");r.push("/dashboard");r.refresh()}catch(err){setError(err instanceof Error?err.message:"Unable to sign in")}finally{setLoading(false)}}return <main className="login-page"><div className="login-glow"/><section className="login-panel"><div className="login-logo"><div className="logo-mark">C</div><div><strong>CREDLOCK</strong><small>AFRICA · SIWES PORTAL</small></div></div><div className="login-copy"><span className="eyebrow"><ShieldCheck size={14}/> SECURE WORKSPACE</span><h1>Build skills.<br/><em>Support with confidence.</em></h1><p>Your central workspace for SIWES learning, technical support practice, assessments and performance.</p></div><form onSubmit={submit}><label>Email address</label><input required type="email" placeholder="you@credlock.africa" value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input required type="password" placeholder="Enter your password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="form-error">{error}</div>}<button className="primary-btn" disabled={loading}>{loading?"Signing in…":"Enter workspace"}{!loading&&<ArrowRight size={17}/>}</button></form><div className="secure-note"><LockKeyhole size={14}/> Protected portal access · Session expires automatically</div></section><aside className="login-visual"><div className="visual-grid"/><div className="visual-content"><span className="eyebrow">CREDLOCK AFRICA</span><h2>12 weeks.<br/><strong>One complete journey.</strong></h2><p>Technical support · Cybersecurity · Device management · Reporting</p><div className="visual-stats"><div><strong>12</strong><span>Weeks</span></div><div><strong>06</strong><span>Projects</span></div><div><strong>10</strong><span>Core labs</span></div></div></div></aside></main>}