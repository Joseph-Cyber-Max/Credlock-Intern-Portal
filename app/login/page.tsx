"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";export default function Login(){const r=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");function submit(e:FormEvent){e.preventDefault();if(!email||!password)return setError("Enter your email and password.");localStorage.setItem("credlock_session",JSON.stringify({email,loggedIn:true}));r.push("/dashboard")}return <main className="login"><div className="login-card"><div className="login-brand"><b>C</b><span>CREDLOCK<small>AFRICA · INTERN PORTAL</small></span></div><label>SECURE ACCESS</label><h1>Welcome back</h1><p>Sign in to your SIWES workspace.</p><form onSubmit={submit}><input type="email" placeholder="Email address" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<small className="error">{error}</small>}<button className="login-btn">Sign in</button></form><small>Demo authentication is enabled for the design build.</small></div></main>}