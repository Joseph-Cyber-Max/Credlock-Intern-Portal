"use client";import{useEffect,useState}from"react";export default function Attendance(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch("/api/attendance").then(r=>r.json()).then(setRows)},[]);const present=rows.filter(x=>x.status==="PRESENT").length;const absent=rows.filter(x=>x.status==="ABSENT").length;return <div className="page"><label>ATTENDANCE</label><h1>Attendance</h1><p>Attendance records from the live portal database.</p><div className="stats"><div className="card"><span className="stat-label">PRESENT</span><div className="stat-value">{present}</div></div><div className="card"><span className="stat-label">ABSENT</span><div className="stat-value">{absent}</div></div><div className="card"><span className="stat-label">RECORDS</span><div className="stat-value">{rows.length}</div></div></div><div className="card"><h2>Recent records</h2><div className="data-table"><div className="table-head"><span>Date</span><span>Status</span><span>Note</span></div>{rows.slice(0,20).map(x=><div className="table-row" key={x.id}><span>{new Date(x.date).toLocaleDateString()}</span><span className="pill">{x.status}</span><span>{x.note||"—"}</span></div>)}</div></div></div>}