import{NextResponse}from"next/server";import{db}from"@/lib/db";import{getServerSession}from"@/lib/server-session";export async function GET(){const s=await getServerSession();if(!s)return NextResponse.json({error:"Unauthorized"},{status:401});const rows=await db.project.findMany({include:{intern:{include:{user:{select:{name:true,email:true}}}}},orderBy:{createdAt:"desc"}});return NextResponse.json(rows)}export async function POST(req:Request){const s=await getServerSession();if(!s||s.role==="INTERN")return NextResponse.json({error:"Forbidden"},{status:403});const b=await req.json();if(!b.title||!b.internId)return NextResponse.json({error:"title and internId are required"},{status:400});return NextResponse.json(await db.project.create({data:{title:b.title,description:b.description,internId:b.internId,status:b.status??"TODO"}}),{status:201})}