import{NextResponse}from"next/server";import{db}from"@/lib/db";export async function GET(){const interns=await db.intern.findMany({include:{user:true,enrollments:true,evaluations:true,projects:true},orderBy:{createdAt:"desc"}});return NextResponse.json(interns.map(i=>({id:i.id,name:i.user.name,email:i.user.email,role:i.user.role,department:i.department,progress:i.progress,quizAverage:i.quizAverage,modulesCompleted:i.enrollments.filter(x=>x.completed).length,projects:i.projects.length})))}