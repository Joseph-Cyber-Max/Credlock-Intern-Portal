"use client";import{useEffect,useState}from"react";export default function Tasks(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch("/api/tasks").then(r=>r.json()).then(setRows)},[]);return <div className="page"><label>TASKS & PROJECTS</label><h1>Work queue</h1><p>Assignments are now loaded from the database.</p><div className="card"><h2>Tasks</h2><div className="data-table"><div className="table-head"><span>Task</span><span>Status</span><span>Assigned to</span></div>{rows.map(x=><div className="table-row" key={x.id}><span><b>{x.title}</b><small>{x.description||""}</small></span><span className="pill">{x.status}</span><span>{x.assignedTo?.name||"Unassigned"}</span></div>)}</div>{rows.length===0&&<p>No tasks have been created yet.</p>}</div></div>}