import {NextResponse} from "next/server";import type {NextRequest} from "next/server";export function middleware(req:NextRequest){const path=req.nextUrl.pathname;if(path==="/login"||path.startsWith("/api/health"))return NextResponse.next();const session=req.cookies.get("credlock_session");if(!session&&path.startsWith("/dashboard"))return NextResponse.redirect(new URL("/login",req.url));return NextResponse.next()}export const config={matcher:["/dashboard/:path*","/login","/api/health"]};