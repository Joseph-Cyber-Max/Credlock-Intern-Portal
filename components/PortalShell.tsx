"use client";import{ReactNode,useEffect,useState}from"react";import Link from"next/link";import{usePathname,useRouter}from"next/navigation";import{LayoutDashboard,BookOpenCheck,ClipboardCheck,Smartphone,ShieldCheck,CalendarCheck,CheckSquare,BarChart3,BookOpen,Users,Menu,X,LogOut}from"lucide-react";const links=[["Dashboard","/dashboard",LayoutDashboard],["Learning","/learning",BookOpenCheck],["Assessments","/assessments",ClipboardCheck],["Technical Support","/technical-support",Smartphone],["Cybersecurity","/cybersecurity",ShieldCheck],["Attendance","/attendance",CalendarCheck],["Tasks & Projects","/tasks",CheckSquare],["Reports","/reports",BarChart3],["Knowledge Base","/knowledge-base",BookOpen],["Interns","/interns",Users]] as const;export default function PortalShell({children}:{children:ReactNode}){const path=usePathname(),router=useRouter(),[open,setOpen]=useState(false),[session,setSession]=useState<any>(null);useEffect(()=>{fetch("/api/me").then(x=>x.ok?x.json():null).then(setSession)},[]);async function logout(){await fetch("/api/auth/logout",{method:"POST"});router.push("/login");router.refresh()}return <div className="shell"><aside className={"side "+(open?"open":"")}><div className="brand"><b>C</b><span>CREDLOCK<small>AFRICA · SIWES PORTAL</small></span><button onClick={()=>setOpen(false)}><X/></button></div><div className="user"><div>{session?.name?.slice(0,2).toUpperCase()||"IN"}</div><section><b>{session?.name||"Loading..."}</b><small>{session?.role||"PORTAL USER"}</small></section></div><nav>{links.map(([name,href,Icon])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={"nav-link "+(path===href?"active":"")}><Icon size={15}/><span>{name}</span></Link>)}</nav><button className="signout" onClick={logout}><LogOut size={15}/> Sign out</button></aside>{open&&<button className="backdrop" onClick={()=>setOpen(false)}/>}<main className="content"><header><button className="menu" onClick={()=>setOpen(true)}><Menu/></button><div className="search-top"><input placeholder="Search portal..." /></div><div className="top-actions"><span className="online">System operational</span><div className="top-avatar">{session?.name?.slice(0,2).toUpperCase()||"IN"}</div></div></header>{children}</main></div>}