"use client";import{ReactNode,useEffect,useState}from"react";import Link from"next/link";import{usePathname,useRouter}from"next/navigation";import{LayoutDashboard,BookOpenCheck,ClipboardCheck,Smartphone,ShieldCheck,CalendarCheck,CheckSquare,BarChart3,BookOpen,Users,Menu,X,LogOut,Bell,Search}from"lucide-react";const links=[["Overview","/dashboard",LayoutDashboard],["Learning","/learning",BookOpenCheck],["Assessments","/assessments",ClipboardCheck],["Support Lab","/technical-support",Smartphone],["Cybersecurity","/cybersecurity",ShieldCheck],["Attendance","/attendance",CalendarCheck],["Tasks & Projects","/tasks",CheckSquare],["Reports","/reports",BarChart3],["Knowledge Base","/knowledge-base",BookOpen],["Intern Management","/interns",Users]] as const;export default function PortalShell({children}:{children:ReactNode}){const path=usePathname(),router=useRouter(),[open,setOpen]=useState(false),[session,setSession]=useState<any>(null);useEffect(()=>{fetch("/api/me").then(x=>x.ok?x.json():null).then(setSession)},[]);async function logout(){await fetch("/api/auth/logout",{method:"POST"});router.push("/login");router.refresh()}return <div className="app-shell"><aside className={"sidebar "+(open?"open":"")}><div className="side-brand"><div className="logo-mark">C</div><div><strong>CREDLOCK</strong><small>AFRICA · SIWES PORTAL</small></div><button className="close-side" onClick={()=>setOpen(false)}><X size={18}/></button></div><div className="profile-card"><div className="avatar">{session?.name?.slice(0,2).toUpperCase()||"IN"}</div><div><strong>{session?.name||"Loading..."}</strong><small>{session?.role||"PORTAL USER"}</small></div><span className="status-dot"/></div><nav>{links.map(([name,href,Icon])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={"nav-item "+(path===href?"active":"")}><Icon size={17}/><span>{name}</span></Link>)}</nav><button className="logout" onClick={logout}><LogOut size={16}/>Sign out</button></aside>{open&&<button className="mobile-overlay" onClick={()=>setOpen(false)}/>}<main className="main-area"><header className="topbar"><button className="mobile-menu" onClick={()=>setOpen(true)}><Menu/></button><div className="global-search"><Search size={16}/><input placeholder="Search interns, lessons, tickets..." /></div><div className="top-right"><button className="icon-btn"><Bell size={17}/><i/></button><span className="system-status">All systems operational</span><div className="top-avatar">{session?.name?.slice(0,2).toUpperCase()||"IN"}</div></div></header>{children}</main></div>}